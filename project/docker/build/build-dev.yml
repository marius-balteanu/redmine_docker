version: '2.1'

services:
  web_development:
    build:
      args:
        HOST_USER_UID: ${HOST_USER_UID}
        HOST_USER_GID: ${HOST_USER_GID}
      context: ../..
      dockerfile: ./docker/services/web/Dockerfile.development
    image: my_redmine_development
